{
  "name": "@metardu/engine",
  "version": "0.1.0",
  "description": "Metardu surveying computation engine — traverse adjustment, COGO, LSA, TIN, coordinate transforms. Shared between metardu web, desktop, and access (mobile).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "author": "error302",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.0"
  }
}
